defmodule Enigma.Plugboard do
  def transform(_single_character_input, _plugboard_config) do
    # take the given character and swap it according to how the plugboard is configured
    # returns the same character if it is not configured
  end

  def setup_plugboard(options \\ []) do
    options
  end
end
